# GeoLoom V4 去模板化重构 — 分阶段开发计划

> 本计划基于 [去模板化Agent架构设计.md](./去模板化Agent架构设计.md) 终版方案。
> 每个阶段拥有独立的目标、准入条件、改动清单、验收标准，可以由 AI 逐阶段执行。
> **总体原则：渐进式改造，每个阶段结束后系统都保持可运行状态，不做大爆炸重构。**

---

## 阶段概览

| 阶段 | 名称 | 核心目标 | 风险 | 预计影响文件 |
|:--|:--|:--|:--|:--|
| 0 | 埋点与可观测性 | 不改行为，只加度量 | 🟢 极低 | 3 文件 |
| 1 | Prompt 清洗 | 删除"专家"角色和模板驱动纪律 | 🟡 中 | 1 文件 |
| 2 | NL-Contract 类型系统 | 引入契约数据结构 | 🟢 低 | 3 新文件 |
| 3 | Clarification Guard | 强制澄清拦截器 | 🟡 中 | 2 文件 |
| 4 | RequirementResolver | 确定性证据选择器 | 🟡 中 | 2 新文件 |
| 5 | Fast Track 旁路 | 查询型请求跳过 Agentic Loop | 🔴 高 | 1 核心文件 |
| 6 | Synthesis 独立化 | 合成回答与 NL-Contract 解耦 | 🟡 中 | 1 核心文件 |
| 7 | Deep Track 逃逸阀 | 复杂场景限制 Agentic 轮次 | 🟡 中 | 1 核心文件 |
| 8 | 全面切换与指标验收 | 统一入口、回归测试 | 🔴 高 | 多文件 |

---

## 阶段 0：埋点与可观测性

### 目标
不改变任何业务行为。在现有流程中埋入性能和质量度量指标，作为后续阶段的基线。

### 准入条件
- 当前系统可正常运行
- 已有 `RuntimeMetrics` 指标框架

### 改动清单

#### 0.1 扩展 `RuntimeMetrics`
**文件**: `backend/src/metrics/RuntimeMetrics.ts`

新增以下指标采集：
```typescript
// 在 recordRequest 方法中扩展
interface RequestMetricEntry {
  // ... 现有字段
  intentMs: number           // Phase 1 意图理解耗时
  evidenceRuntimeMs: number  // Phase 2 取证耗时（不含 LLM）
  synthesisMs: number        // Phase 3 合成耗时
  llmRoundCount: number      // LLM 调用总轮次
  toolCallCount: number      // 工具调用总次数
  unnecessaryAnalysis: boolean  // 回答是否包含用户未请求的分析
}
```

#### 0.2 在 `GeoLoomAgent.handle()` 中打时间戳
**文件**: `backend/src/agent/GeoLoomAgent.ts`

在 `handle()` 方法的关键节点插入计时：
```typescript
// L1211 后
const intentStartedAt = Date.now()

// L1237 后（resolveIntent 返回后）
const intentMs = Date.now() - intentStartedAt

// L1357 前（进入 FunctionCallingLoop 前）
const evidenceStartedAt = Date.now()

// L1391 后（FunctionCallingLoop 返回后）
const evidenceRuntimeMs = Date.now() - evidenceStartedAt

// L1500 前（进入 synthesizeGroundedAnswer 前）
const synthesisStartedAt = Date.now()

// L1509 后（synthesis 返回后）
const synthesisMs = Date.now() - synthesisStartedAt
```

#### 0.3 新增"不必要分析"检测器
**文件**: 新建 `backend/src/metrics/UnnecessaryAnalysisDetector.ts`

```typescript
// 检测回答是否包含用户未请求的关键词
export function detectUnnecessaryAnalysis(input: {
  rawQuery: string
  answer: string
  intent: { queryType: string }
}): boolean {
  // 如果用户没问"机会/异常/投资/开店"，但回答里出现了这些词，标记为不必要分析
  const unnecessaryPatterns = ['机会点', '异常点', '投资', '开店建议', '发展潜力', '值得关注']
  const queryMentions = unnecessaryPatterns.some(p => input.rawQuery.includes(p))
  if (queryMentions) return false
  return unnecessaryPatterns.some(p => input.answer.includes(p))
}
```

### 验收标准
- [ ] 运行现有测试套件全部通过
- [ ] 在日志中能看到 `intentMs`、`evidenceRuntimeMs`、`synthesisMs`、`llmRoundCount` 指标
- [ ] 能输出 `unnecessaryAnalysis` 布尔值
- [ ] 不改变任何 API 响应内容

---

## 阶段 1：Prompt 清洗

### 目标
精简 `AlivePromptBuilder` 的 Agent Contract，删除"专家"角色定位和模板驱动纪律，保留最核心的守则。

### 准入条件
- 阶段 0 完成，有基线指标

### 改动清单

#### 1.1 重写 `AlivePromptBuilder.build()` 的 Agent Contract
**文件**: `backend/src/agent/AlivePromptBuilder.ts`

当前 Agent Contract 包含约 30 行纪律文本（L32-L65），需精简为约 8 行核心守则：

**删除**（这些会导致模板化）：
- 所有以 `'area insight 默认需要...'` 开头的 template 调度指令
- 所有以 `'当前区域 / map_view 的 area insight，优先用这些 template...'` 开头的全家桶指令
- `'先拿结构证据，再决定是否补充语义辅助证据'` — 这类编排纪律应由 RequirementResolver 接管
- `'调用 postgis.execute_spatial_sql 时，常见任务优先使用 payload.template'` — 同上
- 关于 `semantic_selector` 何时调用的 5 行指令 — 同上
- 关于 `spatial_encoder` 何时调用的 4 行指令 — 同上

**保留并改写**：
```
'你是 GeoLoom V4 的空间智能助手。'
'你是一个绝对客观的事实陈述器，不是城市规划师也不是分析专家。'
'所有结论都必须对应真实的工具返回证据。'
'证据不足时先澄清，不允许脑补任何数据。'
'用户问什么就回答什么，不要自作主张地扩展分析范围。'
'严禁输出用户没有请求的"机会点"、"异常点"、"投资建议"。'
'如果底层数据没有呈现，直接说明"未包含相关数据"。'
'回答要求：忠于原问题，客观陈述，不推演。'
```

#### 1.2 精简 `buildToolLoopUserMessage()`
**文件**: `backend/src/agent/GeoLoomAgent.ts` (L1951-L1989)

删除 `area_overview` 分支中的 template 列举和 `semantic_selector` 编排指令。改为：
```typescript
if (input.intent.queryType === 'area_overview') {
  lines.push('编排要求：这是区域洞察题，围绕用户原问题按需取证，不要超范围分析。')
}
```

### 验收标准
- [ ] 运行现有测试套件全部通过
- [ ] Agent Contract 从 ~30 行缩减至 ~8 行
- [ ] 对比阶段 0 基线，`unnecessaryAnalysis` 比例下降
- [ ] area_overview 查询仍然能返回有意义的回答（功能不退化）

---

## 阶段 2：NL-Contract 类型系统

### 目标
引入 `NLContract` 数据结构和编译器，为后续阶段提供类型基础。此阶段**只定义类型和编译逻辑，不改变执行链路**。

### 准入条件
- 阶段 1 完成

### 改动清单

#### 2.1 定义 NL-Contract 类型
**文件**: 新建 `backend/src/contract/types.ts`

```typescript
export type ContractScope = 'viewport' | 'place' | 'user_location' | 'drawn_region'
export type ContractDepth = 'lookup' | 'descriptive' | 'structural' | 'prescriptive'

export interface NLContractMeta {
  scope: ContractScope
  depth: ContractDepth
  forbiddenBlocks: string[]
  estimatedAtomCount: number
  trackingId: string
}

export interface NLContract {
  /** 人类 / LLM 可读的自然语言目标（保真透传） */
  narrative: string
  /** 机器可读的薄壳（用于可观测性、断言、分流） */
  meta: NLContractMeta
}
```

#### 2.2 实现 NL-Contract 编译器
**文件**: 新建 `backend/src/contract/NLContractCompiler.ts`

两种编译路径：
1. **规则编译（Fast Path）**：从 `DeterministicIntent` 直接推导，无 LLM
2. **LLM 编译（Slow Path）**：复杂/模糊问题交给 LLM 生成

```typescript
import type { DeterministicIntent } from '../chat/types.js'
import type { NLContract, ContractDepth } from './types.js'

export class NLContractCompiler {
  /**
   * 规则编译：从已解析的意图直接推导契约
   * 覆盖 95% 场景，无需 LLM
   */
  compileFromIntent(intent: DeterministicIntent, rawQuery: string): NLContract {
    const depth = this.inferDepth(intent, rawQuery)
    const scope = this.inferScope(intent)
    const forbiddenBlocks = this.inferForbiddenBlocks(intent, rawQuery, depth)
    const narrative = this.buildNarrative(intent, rawQuery, forbiddenBlocks)

    return {
      narrative,
      meta: {
        scope,
        depth,
        forbiddenBlocks,
        estimatedAtomCount: this.estimateAtomCount(depth),
        trackingId: `contract_${Date.now()}`,
      },
    }
  }

  private inferDepth(intent: DeterministicIntent, rawQuery: string): ContractDepth {
    if (['nearby_poi', 'nearest_station'].includes(intent.queryType)) {
      return 'lookup'
    }
    if (intent.queryType === 'area_overview') {
      // 仅问"是什么" → descriptive；问"分布/结构" → structural；问"值不值得/适合" → prescriptive
      if (/适合|值不值得|行不行|开店|投资|竞争/u.test(rawQuery)) return 'prescriptive'
      if (/分布|结构|热点|聚集|密度/u.test(rawQuery)) return 'structural'
      return 'descriptive'
    }
    if (intent.queryType === 'compare_places') return 'structural'
    if (intent.queryType === 'similar_regions') return 'structural'
    return 'descriptive'
  }

  private inferScope(intent: DeterministicIntent): NLContract['meta']['scope'] {
    if (intent.anchorSource === 'map_view') return 'viewport'
    if (intent.anchorSource === 'user_location') return 'user_location'
    return 'place'
  }

  private inferForbiddenBlocks(
    intent: DeterministicIntent,
    rawQuery: string,
    depth: ContractDepth,
  ): string[] {
    const forbidden: string[] = []
    // 用户没问"机会/投资"，就禁止输出
    if (!/机会|投资|商机|前景/u.test(rawQuery)) forbidden.push('投资机会')
    if (!/异常|风险|问题/u.test(rawQuery)) forbidden.push('异常点')
    if (!/开店|选址|适合/u.test(rawQuery)) forbidden.push('开店建议')
    if (!/发展|潜力|趋势/u.test(rawQuery)) forbidden.push('发展潜力')
    // lookup 深度禁止所有分析性输出
    if (depth === 'lookup') {
      forbidden.push('业态分析', '片区总结', '热点分析')
    }
    return [...new Set(forbidden)]
  }

  private buildNarrative(
    intent: DeterministicIntent,
    rawQuery: string,
    forbiddenBlocks: string[],
  ): string {
    const forbiddenClause = forbiddenBlocks.length > 0
      ? `\n🚧 绝对约束：不要涉及${forbiddenBlocks.join('、')}，仅基于底层数据客观陈述。`
      : ''
    return `当前任务：忠实回答用户问题「${rawQuery}」。${forbiddenClause}`
  }

  private estimateAtomCount(depth: ContractDepth): number {
    switch (depth) {
      case 'lookup': return 2
      case 'descriptive': return 3
      case 'structural': return 6
      case 'prescriptive': return 7
      default: return 3
    }
  }
}
```

#### 2.3 Shadow Mode 对比日志
**文件**: `backend/src/agent/GeoLoomAgent.ts`

在 `handle()` 中、`resolveIntent()` 之后，**仅写日志**，不改执行链路：
```typescript
// 在 L1238 后插入
const contractCompiler = new NLContractCompiler()
const shadowContract = contractCompiler.compileFromIntent(intent, lastUserText)
logger.info('shadow_nl_contract', {
  narrative: shadowContract.narrative,
  meta: shadowContract.meta,
  actualQueryType: intent.queryType,
})
```

### 验收标准
- [ ] `NLContract` 类型和 `NLContractCompiler` 有完整单元测试
- [ ] 测试覆盖所有 `queryType` × `depth` 组合
- [ ] 日志中能看到 shadow contract 输出
- [ ] 不改变任何执行路径

---

## 阶段 3：Clarification Guard

### 目标
在 `resolveIntent()` 前插入 `IntentAlignmentGuard`，实现有条件的澄清拦截。

### 准入条件
- 阶段 2 完成

### 改动清单

#### 3.1 实现 IntentAlignmentGuard
**文件**: 新建 `backend/src/agent/IntentAlignmentGuard.ts`

```typescript
import type { ChatRequestV4 } from '../chat/types.js'

export interface ClarificationResult {
  needsClarification: boolean
  reason: string | null
  param: string | null
  /** 如果不需要 Clarification，返回推导出的 scope 信息 */
  resolvedScope: {
    kind: 'viewport' | 'boundary' | 'drawn_region' | 'user_location' | 'ambiguous'
    hasExplicitBounds: boolean
  } | null
}

export class IntentAlignmentGuard {
  /**
   * 有条件的严格：
   * - 有 viewport/boundary/region → 通过
   * - 只有 user_location 没有 radius 且问"附近" → 弹窗
   * - 完全没有空间上下文 → 弹窗
   */
  evaluate(input: {
    rawQuery: string
    hasViewport: boolean
    hasBoundary: boolean
    hasDrawnRegion: boolean
    hasUserLocation: boolean
    hasExplicitRadius: boolean
  }): ClarificationResult {
    // 有明确边界 → 直接通过
    if (input.hasDrawnRegion) {
      return { needsClarification: false, reason: null, param: null, resolvedScope: { kind: 'drawn_region', hasExplicitBounds: true } }
    }
    if (input.hasBoundary) {
      return { needsClarification: false, reason: null, param: null, resolvedScope: { kind: 'boundary', hasExplicitBounds: true } }
    }
    if (input.hasViewport) {
      return { needsClarification: false, reason: null, param: null, resolvedScope: { kind: 'viewport', hasExplicitBounds: true } }
    }

    // 有位置但问"附近"类问题且无 radius
    const isNearbyQuery = /附近|周边|旁边|身边|这里/u.test(input.rawQuery)
    if (input.hasUserLocation && isNearbyQuery && !input.hasExplicitRadius) {
      return {
        needsClarification: true,
        reason: '需要确认"附近"的具体范围，例如500米还是骑行10分钟。',
        param: 'search_radius',
        resolvedScope: null,
      }
    }
    if (input.hasUserLocation) {
      return { needsClarification: false, reason: null, param: null, resolvedScope: { kind: 'user_location', hasExplicitBounds: false } }
    }

    // 完全没有空间上下文
    const isExplicitPlaceQuery = /在.{2,}|去.{2,}/.test(input.rawQuery)
    if (isExplicitPlaceQuery) {
      return { needsClarification: false, reason: null, param: null, resolvedScope: { kind: 'ambiguous', hasExplicitBounds: false } }
    }

    return {
      needsClarification: true,
      reason: '当前没有空间上下文。请在地图上选中一个区域，或者告诉我一个明确地点。',
      param: 'spatial_context',
      resolvedScope: null,
    }
  }
}
```

#### 3.2 集成到 `GeoLoomAgent.handle()`
**文件**: `backend/src/agent/GeoLoomAgent.ts`

在 `handle()` 中、`resolveIntent()` 之前插入 Guard 检查：
```typescript
// 在 L1231 前插入
const guard = new IntentAlignmentGuard()
const guardResult = guard.evaluate({
  rawQuery: lastUserText,
  hasViewport: Boolean(readMapViewAnchor(request)),
  hasBoundary: Boolean(readSpatialContext(request)?.boundary),
  hasDrawnRegion: readRequestRegions(request).length > 0,
  hasUserLocation: Boolean(readUserLocation(request)),
  hasExplicitRadius: Number.isFinite(Number(readSpatialContext(request)?.radius)),
})

if (guardResult.needsClarification) {
  // 构建澄清 intent 并直接返回
  const clarificationIntent: DeterministicIntent = {
    ...fallbackIntent,
    needsClarification: true,
    clarificationHint: guardResult.reason,
  }
  await this.finishWithoutEvidence({
    writer, answer: guardResult.reason!, intent: clarificationIntent,
    state, startedAt, providerReady: this.provider.isReady(),
  })
  return
}
```

### 验收标准
- [ ] `IntentAlignmentGuard` 有完整单元测试
- [ ] 有 viewport 时"附近有什么咖啡店"不弹窗
- [ ] 无 viewport 无位置时"附近有什么"触发弹窗
- [ ] 前端能正确渲染 clarification 弹窗（需确认前端已支持）
- [ ] 所有现有测试通过

---

## 阶段 4：RequirementResolver

### 目标
实现确定性证据选择器，从 `NLContract.meta.depth` 直接推导需要调用的原子探针列表。

### 准入条件
- 阶段 2 完成（NLContract 类型可用）

### 改动清单

#### 4.1 定义 Evidence Atom 类型
**文件**: 新建 `backend/src/evidence/atoms.ts`

```typescript
export type EvidenceAtom =
  | 'anchor.resolved'
  | 'anchor.secondary_resolved'
  | 'poi.nearby_list'
  | 'poi.nearest_station'
  | 'area.category_histogram'
  | 'area.representative_samples'
  | 'area.hotspots'
  | 'area.ring_distribution'
  | 'area.competition_density'
  | 'area.aoi_context'
  | 'area.landuse_context'
  | 'area.focused_samples'
  | 'area.region_encoding'
  | 'poi.profile_encoding'
  | 'compare.pairs'

/**
 * 每个 Atom 对应的工具调用参数
 */
export interface AtomExecutionSpec {
  atom: EvidenceAtom
  skill: string
  action: string
  payloadTemplate: Record<string, unknown>
  /** 依赖的前置 atom（必须先完成） */
  dependsOn: EvidenceAtom[]
  /** 是否可以和无依赖的其他 atom 并行 */
  parallelizable: boolean
}
```

#### 4.2 实现 RequirementResolver
**文件**: 新建 `backend/src/evidence/RequirementResolver.ts`

```typescript
import type { NLContract } from '../contract/types.js'
import type { DeterministicIntent } from '../chat/types.js'
import type { EvidenceAtom, AtomExecutionSpec } from './atoms.js'

export interface ResolvedRequirements {
  requiredAtoms: EvidenceAtom[]
  optionalAtoms: EvidenceAtom[]
  executionSpecs: AtomExecutionSpec[]
  confidence: 'high' | 'medium' | 'low'
  /** 建议走 Fast Track 还是 Deep Track */
  recommendedTrack: 'fast' | 'deep'
}

export class RequirementResolver {
  resolve(input: {
    contract: NLContract
    intent: DeterministicIntent
  }): ResolvedRequirements {
    const { depth, scope } = input.contract.meta
    const { queryType } = input.intent

    // lookup 型：最小集
    if (depth === 'lookup') {
      if (queryType === 'nearest_station') {
        return this.buildResult(['anchor.resolved', 'poi.nearest_station'], [], 'fast')
      }
      return this.buildResult(['anchor.resolved', 'poi.nearby_list'], [], 'fast')
    }

    // compare 型
    if (queryType === 'compare_places') {
      return this.buildResult(
        ['anchor.resolved', 'anchor.secondary_resolved', 'compare.pairs'],
        [],
        'fast',
      )
    }

    // similar_regions 型
    if (queryType === 'similar_regions') {
      return this.buildResult(
        ['anchor.resolved', 'area.category_histogram', 'area.representative_samples'],
        ['area.region_encoding'],
        'deep',
      )
    }

    // descriptive 型
    if (depth === 'descriptive') {
      return this.buildResult(
        ['area.category_histogram', 'area.representative_samples'],
        ['area.aoi_context'],
        'fast',
      )
    }

    // structural 型
    if (depth === 'structural') {
      return this.buildResult(
        [
          'area.category_histogram',
          'area.representative_samples',
          'area.hotspots',
          'area.ring_distribution',
          'area.aoi_context',
          'area.landuse_context',
        ],
        ['area.region_encoding'],
        'fast',
      )
    }

    // prescriptive 型
    if (depth === 'prescriptive') {
      return this.buildResult(
        [
          'area.category_histogram',
          'area.representative_samples',
          'area.hotspots',
          'area.competition_density',
          'area.aoi_context',
          'area.landuse_context',
        ],
        ['area.ring_distribution', 'area.region_encoding'],
        'fast',
      )
    }

    // 兜底
    return this.buildResult(
      ['area.category_histogram', 'area.representative_samples'],
      [],
      'fast',
    )
  }

  private buildResult(
    required: EvidenceAtom[],
    optional: EvidenceAtom[],
    track: 'fast' | 'deep',
  ): ResolvedRequirements {
    return {
      requiredAtoms: required,
      optionalAtoms: optional,
      executionSpecs: this.buildSpecs([...required, ...optional]),
      confidence: track === 'fast' ? 'high' : 'medium',
      recommendedTrack: track,
    }
  }

  private buildSpecs(atoms: EvidenceAtom[]): AtomExecutionSpec[] {
    // 每个 atom 映射到具体的 skill/action/template
    return atoms.map(atom => this.atomToSpec(atom))
  }

  private atomToSpec(atom: EvidenceAtom): AtomExecutionSpec {
    const ATOM_SPEC_MAP: Record<EvidenceAtom, Omit<AtomExecutionSpec, 'atom'>> = {
      'anchor.resolved': {
        skill: 'postgis', action: 'resolve_anchor',
        payloadTemplate: { role: 'primary' },
        dependsOn: [], parallelizable: false,
      },
      'anchor.secondary_resolved': {
        skill: 'postgis', action: 'resolve_anchor',
        payloadTemplate: { role: 'secondary' },
        dependsOn: [], parallelizable: true,
      },
      'poi.nearby_list': {
        skill: 'postgis', action: 'execute_spatial_sql',
        payloadTemplate: { template: 'nearby_poi', limit: 10 },
        dependsOn: ['anchor.resolved'], parallelizable: false,
      },
      'poi.nearest_station': {
        skill: 'postgis', action: 'execute_spatial_sql',
        payloadTemplate: { template: 'nearest_station', limit: 3 },
        dependsOn: ['anchor.resolved'], parallelizable: false,
      },
      'area.category_histogram': {
        skill: 'postgis', action: 'execute_spatial_sql',
        payloadTemplate: { template: 'area_category_histogram', limit: 8 },
        dependsOn: [], parallelizable: true,
      },
      'area.representative_samples': {
        skill: 'postgis', action: 'execute_spatial_sql',
        payloadTemplate: { template: 'area_representative_sample', limit: 18 },
        dependsOn: [], parallelizable: true,
      },
      'area.hotspots': {
        skill: 'postgis', action: 'execute_spatial_sql',
        payloadTemplate: { template: 'area_h3_hotspots', limit: 5 },
        dependsOn: [], parallelizable: true,
      },
      'area.ring_distribution': {
        skill: 'postgis', action: 'execute_spatial_sql',
        payloadTemplate: { template: 'area_ring_distribution', limit: 8 },
        dependsOn: [], parallelizable: true,
      },
      'area.competition_density': {
        skill: 'postgis', action: 'execute_spatial_sql',
        payloadTemplate: { template: 'area_competition_density', limit: 8 },
        dependsOn: [], parallelizable: true,
      },
      'area.aoi_context': {
        skill: 'postgis', action: 'execute_spatial_sql',
        payloadTemplate: { template: 'area_aoi_context', limit: 5 },
        dependsOn: [], parallelizable: true,
      },
      'area.landuse_context': {
        skill: 'postgis', action: 'execute_spatial_sql',
        payloadTemplate: { template: 'area_landuse_context', limit: 6 },
        dependsOn: [], parallelizable: true,
      },
      'area.focused_samples': {
        skill: 'semantic_selector', action: 'select_area_evidence',
        payloadTemplate: {},
        dependsOn: ['area.category_histogram', 'area.representative_samples'],
        parallelizable: false,
      },
      'area.region_encoding': {
        skill: 'spatial_encoder', action: 'encode_region_snapshot',
        payloadTemplate: {},
        dependsOn: ['area.category_histogram', 'area.hotspots'],
        parallelizable: false,
      },
      'poi.profile_encoding': {
        skill: 'spatial_encoder', action: 'encode_poi_profile',
        payloadTemplate: {},
        dependsOn: ['area.representative_samples'],
        parallelizable: false,
      },
      'compare.pairs': {
        skill: 'postgis', action: 'execute_spatial_sql',
        payloadTemplate: { template: 'compare_places' },
        dependsOn: ['anchor.resolved', 'anchor.secondary_resolved'],
        parallelizable: false,
      },
    }

    return { atom, ...ATOM_SPEC_MAP[atom] }
  }
}
```

### 验收标准
- [ ] `RequirementResolver` 有完整单元测试
- [ ] 覆盖所有 `depth × queryType` 组合
- [ ] 每种组合的 atom 列表与当前 `recoverCoreSpatialEvidenceIfNeeded()` 的行为一致（不多不少）
- [ ] Shadow 日志对比：Resolver 选择的 atoms vs 实际 Agentic Loop 调用的 tools

---

## 阶段 5：Fast Track 旁路

### 目标
**核心改造**。当 `recommendedTrack === 'fast'` 时，跳过 `FunctionCallingLoop`，直接用 `RequirementResolver` 的 atom 列表做确定性并行执行。

### 准入条件
- 阶段 3 + 阶段 4 完成
- Shadow 日志确认 Resolver 选择与实际行为一致

### 改动清单

#### 5.1 实现 `DeterministicEvidenceRuntime`
**文件**: 新建 `backend/src/evidence/DeterministicEvidenceRuntime.ts`

核心逻辑：
1. 接收 `AtomExecutionSpec[]`
2. 按 `dependsOn` 拓扑排序，划分并行 wave
3. 每个 wave 内的 atom 用 `Promise.all` 并行执行
4. 收集所有结果组装为 `ToolExecutionTrace[]`

```typescript
export class DeterministicEvidenceRuntime {
  async execute(input: {
    specs: AtomExecutionSpec[]
    intent: DeterministicIntent
    state: AgentTurnState
    context: SkillExecutionContext
    writer: SSEWriter
  }): Promise<ToolExecutionTrace[]> {
    const waves = this.buildWaves(input.specs)
    const traces: ToolExecutionTrace[] = []

    for (const wave of waves) {
      await input.writer.stage('tool_run')
      const waveResults = await Promise.all(
        wave.map(spec => this.executeAtom(spec, input))
      )
      traces.push(...waveResults)
    }

    return traces
  }

  private buildWaves(specs: AtomExecutionSpec[]): AtomExecutionSpec[][] {
    // 拓扑排序，按 dependsOn 分层
    // ...
  }

  private async executeAtom(
    spec: AtomExecutionSpec,
    input: { ... },
  ): Promise<ToolExecutionTrace> {
    // 复用现有 executeToolCall 逻辑
    // ...
  }
}
```

#### 5.2 改造 `GeoLoomAgent.handle()` 主路径
**文件**: `backend/src/agent/GeoLoomAgent.ts`

在现有 `runFunctionCallingLoop` 调用位置（L1355-L1391），加入 Fast Track 分支：

```typescript
// 在 L1354 后替换为：
const contract = contractCompiler.compileFromIntent(intent, lastUserText)
const requirements = requirementResolver.resolve({ contract, intent })

let execution: FunctionCallingLoopResult
let toolLoopUsedFallbackProvider = false

if (requirements.recommendedTrack === 'fast') {
  // Fast Track：确定性并行执行，零 LLM
  const fastRuntime = new DeterministicEvidenceRuntime()
  const traces = await fastRuntime.execute({
    specs: requirements.executionSpecs,
    intent, state, context: skillContext, writer,
  })
  state.toolCalls.push(...traces)
  execution = { assistantMessage: null, traces }
} else {
  // Deep Track：保留现有 Agentic Loop（后续阶段 7 会限制轮次）
  execution = await runFunctionCallingLoop({ ... })
}
```

### 验收标准
- [ ] `nearby_poi` 和 `nearest_station` 查询走 Fast Track，零 LLM 调用
- [ ] `descriptive` 级 area_overview 查询走 Fast Track
- [ ] Fast Track 的 `evidenceRuntimeMs` 比 Agentic Loop 快 50%+
- [ ] 回答质量不退化（需人工抽样 10 组对比）
- [ ] 所有现有测试通过

---

## 阶段 6：Synthesis 独立化

### 目标
将 `synthesizeGroundedAnswer()` 改为接收 `NLContract.narrative` 作为约束，取代从 `intent` 重建 synthesis prompt 的方式。

### 准入条件
- 阶段 5 完成

### 改动清单

#### 6.1 改造 `synthesizeGroundedAnswer()`
**文件**: `backend/src/agent/GeoLoomAgent.ts` (L3335-L3391)

修改签名，新增 `contract` 参数：
```typescript
private async synthesizeGroundedAnswer(input: {
  provider: LLMProvider
  contract: NLContract    // ← 新增
  intent: DeterministicIntent
  evidenceView: EvidenceView
  rendered: RenderedAnswer
  spatialConstraint: SpatialAnalysisConstraint | null
  rawQuery: string
}) {
  // 使用 contract.narrative 替代硬编码的 synthesis 指令
  const synthesisPrompt = [
    `问题：${input.rawQuery}`,
    `任务契约：${input.contract.narrative}`,
    `范围：${this.describeSpatialConstraint(input.spatialConstraint)}`,
    '只基于下面证据写最终回答。',
    // ... 铁律
    '证据：',
    ...evidenceLines.map(line => `- ${line}`),
  ].join('\n')
}
```

#### 6.2 扩展 Synthesis 到所有 queryType
当前 `synthesizeGroundedAnswer` 仅对 `area_overview` 生效（L3344）。改为：
- `area_overview` → 保持
- `nearby_poi` / `nearest_station` → Fast Track 直接用 `rendered.answer`，跳过 Synthesis
- `compare_places` / `similar_regions` → 启用 Synthesis

### 验收标准
- [ ] `synthesizeGroundedAnswer` 使用 `NLContract.narrative` 作为约束
- [ ] `compare_places` 查询的回答质量提升（不再依赖纯模板）
- [ ] `forbiddenBlocks` 中的禁止词不出现在回答中
- [ ] 所有现有测试通过

---

## 阶段 7：Deep Track 逃逸阀

### 目标
对走 Deep Track 的复杂场景，限制 Agentic Loop 最多 1 轮 LLM 补充。

### 准入条件
- 阶段 5 完成

### 改动清单

#### 7.1 改造 Deep Track 分支
**文件**: `backend/src/agent/GeoLoomAgent.ts`

```typescript
if (requirements.recommendedTrack === 'deep') {
  // Step 1: 先走确定性执行（同 Fast Track）
  const fastRuntime = new DeterministicEvidenceRuntime()
  const deterministicTraces = await fastRuntime.execute({
    specs: requirements.executionSpecs.filter(s => s.dependsOn.length === 0),
    intent, state, context: skillContext, writer,
  })
  state.toolCalls.push(...deterministicTraces)

  // Step 2: 检查 Evidence Packet 是否足够
  const evidenceGap = this.assessEvidenceGap(state, requirements)

  if (evidenceGap.needsSupplement) {
    // Step 3: 最多 1 轮 LLM 补充（逃逸阀）
    execution = await runFunctionCallingLoop({
      ...existingOptions,
      maxRounds: 1,  // ← 关键：限制为 1 轮
      messages: [
        { role: 'system', content: `你已经拿到了这些证据：${evidenceGap.availableAtoms.join(', ')}。请判断还需要补什么。` },
        { role: 'user', content: toolLoopUserMessage },
      ],
    })
  }
}
```

#### 7.2 调整 `DEFAULT_LLM_ANALYSIS_MAX_ROUNDS`
**文件**: `backend/src/agent/GeoLoomAgent.ts` (L59)

```typescript
// 从 6 降到 2（确定性执行 + 最多 1 轮 LLM 补充 + 1 轮兜底）
const DEFAULT_LLM_ANALYSIS_MAX_ROUNDS = 2
```

### 验收标准
- [ ] Deep Track 场景的 LLM 轮次 ≤ 3（含 Phase 1 和 Phase 3）
- [ ] `prescriptive` 级查询仍然能返回有质量的分析
- [ ] `area_overview` P95 延迟从 ~70s 降到 ≤ 40s

---

## 阶段 8：全面切换与指标验收

### 目标
统一入口、清理遗留代码、回归测试、验收指标。

### 准入条件
- 阶段 5、6、7 全部完成

### 改动清单

#### 8.1 清理遗留代码
- 删除 `recoverCoreSpatialEvidenceIfNeeded()` — 被 `DeterministicEvidenceRuntime` 替代
- 删除 `AREA_INSIGHT_TEMPLATES` / `AREA_INSIGHT_CORE_TEMPLATES` / `AREA_INSIGHT_SEMANTIC_TEMPLATES` 常量 — 被 `RequirementResolver` 的 atom spec 替代
- 精简 `buildToolLoopUserMessage()` — 不再需要 template 编排指令

#### 8.2 统一入口
- 所有请求都先走 `IntentAlignmentGuard` → `NLContractCompiler` → `RequirementResolver`
- `FunctionCallingLoop` 仅在 Deep Track 逃逸阀中使用

#### 8.3 回归测试
对现有 `GeoLoomAgent.spec.ts` 中所有 test case 进行回归：
- `nearby_poi` 场景
- `nearest_station` 场景
- `area_overview` 场景
- `compare_places` 场景
- `similar_regions` 场景
- 边界场景：无 viewport、无位置、无锚点

#### 8.4 指标验收

| 指标 | 当前基线（估计） | 阶段 8 目标 |
|:--|:--|:--|
| Fast Track 覆盖率 | 0% | ≥ 90% |
| Deep Track 使用率 | 100% | ≤ 10% |
| LLM 轮次（Fast Track） | 4-7 | ≤ 2 |
| LLM 轮次（Deep Track） | 4-7 | ≤ 3 |
| P95 延迟（nearby_poi） | ~25s | ≤ 15s |
| P95 延迟（area_overview） | ~70s | ≤ 30s |
| 不必要分析率 | ~30-50% | ≤ 5% |

### 验收标准
- [ ] 所有测试通过
- [ ] 指标达标
- [ ] 不必要分析率人工抽样验证 ≤ 5%
- [ ] 无回归 bug

---

## 新增文件汇总

| 文件路径 | 阶段 | 职责 |
|:--|:--|:--|
| `backend/src/metrics/UnnecessaryAnalysisDetector.ts` | 0 | 检测不必要分析 |
| `backend/src/contract/types.ts` | 2 | NLContract 类型定义 |
| `backend/src/contract/NLContractCompiler.ts` | 2 | 契约编译器 |
| `backend/src/agent/IntentAlignmentGuard.ts` | 3 | 澄清拦截器 |
| `backend/src/evidence/atoms.ts` | 4 | 证据原子类型 |
| `backend/src/evidence/RequirementResolver.ts` | 4 | 确定性证据选择器 |
| `backend/src/evidence/DeterministicEvidenceRuntime.ts` | 5 | 确定性并行执行引擎 |

## 改动文件汇总

| 文件路径 | 涉及阶段 | 改动性质 |
|:--|:--|:--|
| `backend/src/metrics/RuntimeMetrics.ts` | 0 | 扩展指标 |
| `backend/src/agent/AlivePromptBuilder.ts` | 1 | 重写 Agent Contract |
| `backend/src/agent/GeoLoomAgent.ts` | 0,1,3,5,6,7,8 | 主流程改造（最核心） |
| `backend/src/llm/FunctionCallingLoop.ts` | 7 | 限制 maxRounds |
