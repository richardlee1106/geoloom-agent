Five Web Workers that run off the main thread. geo.worker handles spatial queries and geometry computation. spiral.worker generates spiral point distributions. gravity.worker simulates gravity-based point clustering. basic.worker provides simple layout algorithms. dataLoader.worker handles streaming data fetching and parsing. Workers communicate with the main thread via Comlink RPC.
