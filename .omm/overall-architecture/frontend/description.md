Vue 3 SPA (Vite + TypeScript) that provides the user-facing map and chat interface. Renders spatial visualizations using deck.gl layers over OpenLayers base maps. Streams agent responses from the backend via SSE, displayed as AI-generated cards in the evidence panel. Frontend runs Web Workers (geo, spiral, gravity, basic, dataLoader) to offload heavy computation off the main thread.
