GeoLoom Agent is a spatial intelligence platform that combines a Vue 3 frontend with a Fastify backend to answer location-aware questions about areas, POIs, and urban features. The system uses a multi-skill agent architecture where a central GeoLoomAgent orchestrates LLM-driven tool calls against spatial databases (PostGIS), semantic vector indexes (FAISS), and encoding services (Python bridge).
