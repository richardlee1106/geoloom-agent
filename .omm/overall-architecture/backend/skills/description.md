SkillRegistry manages all skill plugins. Currently registered skills: postgis (spatial SQL with template engine), spatial_encoder (region/POI feature encoding via Python bridge), spatial_vector (FAISS-based semantic POI and region search), route_distance (OSM-based multi-destination routing). Each skill exposes a manifest (SKILLS/*.md) and implements execute(context, action, payload).
