LLM abstraction layer with swappable providers: AnthropicCompatibleProvider (Anthropic API), OpenAICompatibleProvider (OpenAI-compatible API), InMemoryLLMProvider (mock for testing/fallback). FunctionCallingLoop orchestrates multi-round tool call loops. toolSchemaBuilder generates JSON tool schemas from skill manifests. createDefaultLLMProvider instantiates the configured provider from environment variables.
