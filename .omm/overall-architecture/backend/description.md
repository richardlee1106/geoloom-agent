Fastify-based TypeScript backend that hosts the GeoLoomAgent, skill registry, routes, and integration adapters. Entry point is server.ts which wires up PostGIS pool, skill registry, memory manager, and the chat runtime. Routes are registered under /api/geo/* (chat, geo, skills, spatial) and /api/category/* (category tree). The backend communicates with the frontend exclusively via SSE streaming.
