FAISS-based semantic vector index (RemoteFirstFaissIndex) that supports two actions: search_similar_regions (finds regions with semantically similar POI distributions) and search_semantic_pois (finds POIs matching a semantic query vector). Vector embeddings are pre-computed and stored in the FAISS index. The backend calls this service via HTTP; if unavailable it skips semantic search steps.
