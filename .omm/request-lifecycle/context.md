A user types a spatial query in the AI chat panel. The frontend wraps the query with spatial context (viewport, user location, selected categories, drawn regions) into a ChatRequestV4 and POSTs it to /api/geo/chat. The backend streams back typed SSE events which the frontend renders incrementally: first the intent_preview, then stage transitions with thinking indicators, then tool execution traces, then the final answer.
