The backend connects to five external service categories: a PostGIS database (spatial data source), a Python microservice (spatial encoding), a FAISS microservice (semantic vector search), an OSM routing microservice (route distances), and LLM API providers (Anthropic or OpenAI-compatible). All adapters use a RemoteFirst pattern: try remote first, fall back to local/in-memory/skip behavior on failure.
